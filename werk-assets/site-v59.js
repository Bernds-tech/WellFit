(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.querySelector('.releaseWrap'))return;const [gates,status]=await Promise.all([fetch('./werk-data/publication-gates.json?v=20260901-v59').then(r=>r.json()),fetch('./werk-data/project-status.json?v=20260901-v59').then(r=>r.json())]);const sec=document.querySelector('.page[data-page="transparenz"] .sec');if(!sec)return;const wrap=document.createElement('section');wrap.className='releaseWrap';wrap.innerHTML=`<div class="head"><div><div class="ey">Publikationsreife</div><h2>Eine schöne Seite ist noch keine freigegebene Seite.</h2></div><p>${esc(gates.release_policy)}</p></div><div class="statusLegend"><span class="gateStatus pass">PASS</span><span class="gateStatus partial">PARTIAL</span><span class="gateStatus open">OPEN</span></div><div class="releaseGrid">${gates.gates.map(g=>`<article class="releaseGate"><div class="num">${esc(g.id)}</div><h3>${esc(g.name)}</h3><span class="gateStatus ${esc(g.status)}">${esc(g.status.toUpperCase())}</span><ul>${g.criteria.map(c=>`<li>${esc(c)}</li>`).join('')}</ul></article>`).join('')}</div><div style="height:24px"></div><div class="head"><div><div class="ey">Interner Arbeitsstand</div><h2>Wo das Projekt tatsächlich steht.</h2></div><p>${esc(status.note)}</p></div><div class="progressList">${status.components.map(c=>`<div class="progressRow"><span>${esc(c.name)}</span><div class="progressTrack" aria-label="${esc(c.name)} ${c.estimate_pct} Prozent"><div class="progressFill" style="width:${Math.max(0,Math.min(100,c.estimate_pct))}%"></div></div><b>${c.estimate_pct}%</b></div>`).join('')}</div><div style="height:22px"></div><div class="blockers"><b>Blocker vor öffentlicher Freigabe</b><ul>${status.blocking_items_for_public_release.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`;sec.appendChild(wrap);const ribbon=document.querySelector('.ribbon');if(ribbon)ribbon.textContent='WERK Österreich · V59 · Prüffassung mit Publikationsgates';const footer=document.querySelector('.footerin div:last-child');if(footer)footer.textContent='V59 Prüffassung · noindex · Pflichtgates vor öffentlicher Freigabe';}catch(e){console.error('V59 release dashboard',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();
