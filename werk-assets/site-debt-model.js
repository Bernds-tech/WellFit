(()=>{
const fmt=n=>new Intl.NumberFormat('de-AT',{minimumFractionDigits:1,maximumFractionDigits:3}).format(n);
async function load(){try{if(document.getElementById('debtCalc'))return;const r=await fetch('./werk-data/debt-model.json?v=20260901-v1');if(!r.ok)return;const d=await r.json();const host=document.querySelector('.page[data-page="reformen"] .sec');if(!host)return;const wrap=document.createElement('section');wrap.id='debtCalc';wrap.className='debtCalc';wrap.innerHTML=`<div class="head"><div><div class="ey">DEBT-01 / BUD-02 · Rechenmodell</div><h2>Was bedeutet eine feste Nettotilgung rechnerisch?</h2></div><p>${d.status}. Fakten und Annahmen bleiben getrennt.</p></div><div class="calcGrid"><div class="calcInputs"><div class="calcField"><label>Ausgangsschuld Bund in Mrd. € · Fakt, Datenstand ${d.baseline.data_date}</label><input id="dcStart" type="number" step="0.001" value="${d.baseline.federal_financial_debt_eur_billion}" readonly></div><div class="calcField"><label>Jährliche Nettotilgung in Mrd. € · Szenario</label><input id="dcPay" type="number" min="0" step="0.5" value="${d.default_inputs.annual_net_principal_reduction_eur_billion}"></div><div class="calcField"><label>Betrachtungszeitraum in Jahren</label><input id="dcYears" type="number" min="1" max="50" step="1" value="${d.default_inputs.years}"></div><div class="calcField"><label>Startverzögerung bis Tilgung in Jahren</label><input id="dcDelay" type="number" min="0" max="20" step="1" value="${d.default_inputs.start_delay_years}"></div><div class="calcField"><label>Angenommener vermiedener Refinanzierungszins in % · reine Annahme</label><input id="dcRate" type="number" min="0" max="15" step="0.1" value="${d.default_inputs.assumed_avoided_refinancing_rate_pct}"></div></div><div class="calcResults"><div class="resultGrid"><div class="resultCell"><span>Bruttotilgung im Szenario</span><b id="dcReduction">–</b></div><div class="resultCell"><span>Restschuld nach Zeitraum</span><b id="dcRemaining">–</b></div><div class="resultCell"><span>Schuldenabbau ggü. Start</span><b id="dcPct">–</b></div><div class="resultCell"><span>Vereinfachte kumulierte Zinsdifferenz</span><b id="dcInterest">–</b></div></div><div class="calcWarn">${d.publication_warning}</div><div class="calcRules"><div class="ey">Modellregeln</div><ul class="check">${d.input_rules.map(x=>`<li>${String(x).replace(/[<>]/g,'')}</li>`).join('')}</ul></div></div></div>`;host.appendChild(wrap);const ids=['dcPay','dcYears','dcDelay','dcRate'];const calc=()=>{const start=+document.getElementById('dcStart').value,pay=Math.max(0,+document.getElementById('dcPay').value),years=Math.max(1,Math.round(+document.getElementById('dcYears').value)),delay=Math.max(0,Math.round(+document.getElementById('dcDelay').value)),rate=Math.max(0,+document.getElementById('dcRate').value)/100;let debt=start,reduced=0,interest=0;for(let y=1;y<=years;y++){interest+=reduced*rate;if(y>delay&&debt>0){const cut=Math.min(pay,debt);debt-=cut;reduced+=cut}}document.getElementById('dcReduction').textContent=fmt(reduced)+' Mrd. €';document.getElementById('dcRemaining').textContent=fmt(debt)+' Mrd. €';document.getElementById('dcPct').textContent=fmt(start?reduced/start*100:0)+' %';document.getElementById('dcInterest').textContent=fmt(interest)+' Mrd. €*';};ids.forEach(id=>document.getElementById(id).addEventListener('input',calc));calc()}catch(err){console.error('WERK debt calculator',err)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();