(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('overlapRegister'))return;const r=await fetch('./werk-data/implementation-overlap.json?v=20260902-v1');if(!r.ok)return;const d=await r.json();const host=document.querySelector('.page[data-page="transparenz"] .sec');if(!host)return;const sec=document.createElement('section');sec.id='overlapRegister';sec.className='overlapWrap';sec.innerHTML=`<div class="head"><div><div class="ey">Abgrenzung</div><h2>${esc(d.title)}</h2></div><p>${esc(d.rule)}</p></div><div class="overlapGrid">${d.items.map(x=>`<article class="overlapCard"><div class="overlapHead"><div class="num">${esc(x.reform_id)}</div><h3>${esc(x.reform_id)}</h3></div><div class="overlapBody"><div class="overlapSide"><span>Besteht / läuft bereits</span><p>${esc(x.existing)}</p></div><div class="overlapSide werk"><span>Zusätzlicher WERK-Schritt</span><p>${esc(x.werk_delta)}</p></div></div></article>`).join('')}</div>`;host.appendChild(sec)}catch(e){console.error('WERK overlap register',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();