(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('iwQuality'))return;const r=await fetch('./werk-data/ideenwerk-quality.json?v=20260902-v14');if(!r.ok)throw new Error('ideenwerk-quality '+r.status);const d=await r.json();const host=document.querySelector('.page[data-page="ideenwerk"] .sec');if(!host)return;const sec=document.createElement('section');sec.id='iwQuality';sec.className='iwQuality';sec.innerHTML=`<div class="head"><div><div class="ey">Qualität & Skalierung</div><h2>${esc(d.title)}</h2></div><p>${esc(d.rule)}</p></div><div class="iwQualityTests">${d.prepared_tests.map(t=>`<article class="iwQTest"><span class="iwQStatus">${esc(t.status)}</span><h3>${esc(t.name)}</h3><p>${esc(t.scope)}</p></article>`).join('')}</div><div style="height:18px"></div><div class="table"><table><thead><tr><th>Messgröße</th><th>Warum wichtig?</th><th>Priorität</th><th>Aktueller Wert</th></tr></thead><tbody>${d.metrics.map(m=>`<tr><td><b>${esc(m.label)}</b><br><small>${esc(m.publication)}</small></td><td>${esc(m.meaning)}</td><td><span class="iwPriority ${m.priority==='kritisch'?'critical':''}">${esc(m.priority)}</span></td><td><span class="iwNotMeasured">${esc(m.current)}</span></td></tr>`).join('')}</tbody></table></div><div class="iwQGates"><div class="ey">Freigabegates</div><h3>Keine Qualitätsbehauptung ohne Messung.</h3><ul>${d.release_gates.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`;host.appendChild(sec)}catch(e){console.error('IDEENWERK quality',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();
