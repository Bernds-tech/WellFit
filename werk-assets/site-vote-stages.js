(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('voteStages'))return;const r=await fetch('./werk-data/vote-constitutional-options.json?v=20260902-v1');if(!r.ok)return;const d=await r.json();const host=document.querySelector('.page[data-page="vote"] .sec');if(!host)return;const sec=document.createElement('section');sec.id='voteStages';sec.className='voteStages';sec.innerHTML=`<div class="head"><div><div class="ey">Rechtsstrategie</div><h2>${esc(d.title)}</h2></div><p>${esc(d.principle)}</p></div><div class="stageFlow">${d.recommended_sequence.map(id=>d.options.find(x=>x.id===id)).filter(Boolean).map(x=>`<article class="voteStage" data-stage="${esc(x.id)}"><h3>${esc(x.name)}</h3><p>${esc(x.description)}</p><small>${esc(x.legal_level)}</small><div class="voteStageGate"><b>Gate:</b> ${esc(x.gate)}</div></article>`).join('')}</div><div class="stageNote">${esc(d.policy_note)}</div>`;host.appendChild(sec)}catch(e){console.error('WERK vote stages',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();