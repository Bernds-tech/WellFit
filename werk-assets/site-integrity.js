(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('integrityCheck'))return;const mr=await fetch('./werk-data/preview-manifest.json?v=20260905-v71',{cache:'no-store'});if(!mr.ok)return;const d=await mr.json();const host=document.querySelector('.page[data-page="transparenz"] .sec');if(!host)return;const results=await Promise.all(d.required.map(async x=>{try{const r=await fetch(x.path+'?integrity=20260905-v71',{cache:'no-store'});return {...x,ok:r.ok,status:r.status}}catch(e){return {...x,ok:false,status:'ERR'}}}));const ok=results.filter(x=>x.ok).length,fail=results.length-ok;const sec=document.createElement('section');sec.id='integrityCheck';sec.className='integrityCheck';sec.innerHTML=`<div class="head"><div><div class="ey">Technischer Selbsttest</div><h2>Preview-Integritätscheck</h2></div><p>${esc(d.rule)}</p></div><div class="integritySummary"><div><span>Prüffassung</span><b>${esc(d.current_preview)}</b></div><div><span>Dateien erreichbar</span><b>${ok} / ${results.length}</b></div><div class="${fail?'fail':'pass'}"><span>Fehler</span><b>${fail}</b></div></div><div class="integrityGrid">${results.map(x=>`<div class="integrityRow ${x.ok?'ok':'bad'}"><span>${x.ok?'OK':'FEHLT'}</span><b>${esc(x.group)}</b><code>${esc(x.path)}</code><em>${esc(x.status)}</em></div>`).join('')}</div>`;host.appendChild(sec)}catch(e){console.error('WERK integrity check',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();
