(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('voteFormat'))return;const r=await fetch('./werk-data/vote-format.json?v=20260902-v1');if(!r.ok)return;const d=await r.json();const host=document.querySelector('.page[data-page="vote"] .sec');if(!host)return;const marker=host.querySelector('#voteNotice');const sec=document.createElement('section');sec.id='voteFormat';sec.className='voteFormat';sec.innerHTML=`<div class="voteFormatHead"><div class="num">Rechtsklarheit</div><h3>${esc(d.title)}</h3></div><div class="voteFormatGrid"><div class="voteFormatSide"><span>Amtliches Bundesformat heute</span><h4>Keine eigene dritte Enthaltungsoption</h4><p>${esc(d.federal_current.volksbefragung)}</p><p>${esc(d.federal_current.volksabstimmung)}</p><div class="formatChoices"><span class="formatChoice">Ja / Nein</span><span class="formatChoice">oder 2 Alternativen</span></div></div><div class="voteFormatSide targetFormat"><span>WERK-Ziel für qualifizierte Großfragen</span><h4>Teilnahme ohne Ja-/Nein-Zwang</h4><p>${esc(d.werk_target.reason)}</p><div class="formatChoices">${d.werk_target.qualified_major_question_choices.map(x=>`<span class="formatChoice">${esc(x)}</span>`).join('')}</div><p>${esc(d.werk_target.note)}</p></div></div><div class="formatWarn">${esc(d.publication_rule)}</div>`;if(marker)marker.insertAdjacentElement('afterend',sec);else host.prepend(sec)}catch(e){console.error('WERK vote format',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();