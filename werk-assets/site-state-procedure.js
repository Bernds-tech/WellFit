(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function load(){try{if(document.getElementById('stateProcedure'))return;const r=await fetch('./werk-data/state-procedure-matrix.json?v=20260902-v1');if(!r.ok)return;const d=await r.json();const host=document.querySelector('.page[data-page="regionen"] .sec');if(!host)return;const sec=document.createElement('section');sec.id='stateProcedure';sec.className='stateProcedure';sec.innerHTML=`<div class="head"><div><div class="ey">Verfahrensaudit · Stufe 2</div><h2>${esc(d.title)}</h2></div><p>${esc(d.rule)}</p></div><div class="table"><table><thead><tr><th>Land</th><th>Verfahrensbasis</th><th>Antwortformat</th><th>Bindungswirkung</th><th>Teilgebiet / digital</th></tr></thead><tbody>${d.states.map(s=>`<tr><td><b>${esc(s.name)}</b><br><span class="procStatus ${s.status.startsWith('weitgehend')?'wide':''}">${esc(s.status)}</span></td><td>${esc(s.procedure_law)}<br><a class="procSource" target="_blank" rel="noopener" href="${s.source}">Primärquelle</a></td><td>${esc(s.answer_format)}</td><td>${esc(s.binding_effect)}</td><td>${esc(s.partial_region)}<br><b>Digital:</b> ${esc(s.digital)}</td></tr>`).join('')}</tbody></table></div><div class="procOpen"><b>Gemeinsam noch offen</b><ul>${d.common_open_questions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`;host.appendChild(sec)}catch(e){console.error('WERK state procedure matrix',e)}}
if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',load);else load();
})();