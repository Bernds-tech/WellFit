# WellFit Current State

Last initialized: 2026-08-19

## Project role
This repository owns WellFit visual, landing-page and UI work. Technical web/backend implementation belongs in `Bernds-tech/WellFit-now`; native AR/buddy/mobile implementation belongs in `Bernds-tech/WellFit-Buddy` unless an accepted bridge task explicitly spans repositories.

## Current direction
- Maintain the visual language and landing/product presentation.
- Keep graphics aligned with actual technical capabilities rather than inventing unsupported functionality.
- Treat cross-repository UI/feature requests as change requests first.

## Do not repeat by default
- Do not rebuild technical backend logic here.
- Do not recreate native AR/buddy implementation here.
- Do not introduce a parallel visual system without checking existing design work and accepted scope.

Before changing this file, inspect current `main`, relevant design assets and the corresponding technical state in WellFit-now/WellFit-Buddy when the feature crosses repository boundaries.