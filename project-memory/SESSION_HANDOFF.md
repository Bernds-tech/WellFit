# WellFit Session Handoff

Update this at the end of substantial work or whenever pausing at a non-obvious state.

## Current handoff
- Updated: 2026-09-06 Europe/Vienna
- Active focus: `WFG-RUDI-WORLD-001` / `WFG-CR-008` remains the owner-directed graphical priority. Repository/runtime implementation and pre-Site hardening are complete; the remaining objective is the actual editable `wellfit-bewegt` Site synchronization and visual acceptance.
- DOM-world foundation: WellFit-now PR #401 exact head `54186ec16a617549acbaa0437b0b81ab36ee2abb` passed Build #1285, Container #270, Database #262, Beta Emulator #241 and Project Memory Guard/Quality/Status, then squash-merged as `9ae4f278a90d17d612f0399c40babd32c344e02b`.
- Pre-Site fallback hardening: static audit found that WebGL/GLTF failure or a live reduced-motion/static transition could leave a previously scheduled animated journey completion alive behind the fallback, and the active controller loaded five clips it never selected. WellFit-now PR #402 corrected those bounded defects without changing DOM-world geometry. Exact head `23318cdf395bd25e46f1b2a31499f14cc8afd51d` passed Build #1288, Container #273, Database #265 and Project Memory Guard #116 / Quality #123 / Status #131, then squash-merged as `b07d39938aeab4e32eddac7d19b8e15e22afacb7`. Beta Emulator did not trigger because its workflow is path-filtered to Firebase/mission/package scopes and PR #402 changed only landing Rudi runtime/validator plus Project Memory.
- Hardened runtime behavior: one active `LivingRudiWorld`; `F` as first climb/podium; DOM surfaces including narrow letters/thin ledges; no viewport clamp; surface-relative scroll; full-offscreen + scroll-settle before catch-up; grounded visible walk/climb routes; DOM geometry owns locomotion; CTA gaze does not relocate Rudi; foreground/background layering; Canvas pointer pass-through. Leaving WebGL now cancels pending motion/journeys and clears attention, renderer failure enters the same static state, route guides exist only in WebGL mode, and static fallback remains hidden until a real anchor exists.
- Initial WebGL asset loading is reduced to the five clips the active controller can select: walk, idle, inspect, celebrate and climb. Run/alert/point/jump/sit assets remain materialized for future separately scoped living scenes but are not fetched by the initial active controller.
- Consolidation result: the deleted `LivingRudi3D.tsx` viewport/chapter controller remains forbidden. Coffee/table/lounge scenes from that old controller are not part of the active Runtime claim and may only be rebuilt later on top of the visually accepted DOM world.
- Site-sync contract: `project-memory/RUDI_SITE_SYNC_MANIFEST.json` on this coordination branch is advanced to WellFit-now merge `b07d39938aeab4e32eddac7d19b8e15e22afacb7`, pins updated `LivingRudiWorld.tsx` / validator blobs, preserves the immutable geometry/hero/landing blobs and adds fail-closed static-fallback/loading invariants.
- Locks: original `XLOCK-WF-RUDI-WORLD-20260905` remains RELEASED. The bounded `XLOCK-WF-RUDI-FALLBACK-20260906` is RELEASED after the WellFit-now merge and manifest reconciliation. `LOCK-WFG-RUDI-WORLD-001` remains ACTIVE only for actual Site synchronization/visual acceptance.
- First unproven step: open the exact editable `wellfit-bewegt` ChatGPT Site source and apply the current `RUDI_SITE_SYNC_MANIFEST.json` pinned to WellFit-now merge `b07d39938aeab4e32eddac7d19b8e15e22afacb7`. Preview with real WebGL and execute all ten required checks, including reduced-motion stability with no delayed journey relocation.
- Public-Site boundary: Site version 105 belongs to the earlier renderer generation and is not evidence for the DOM-bound/hardened implementation. GitHub merges do not publish the Site. Do not mark `WFG-RUDI-WORLD-001` ACCEPTED until the exact Site preview passes all required checks and deliberate publication occurs.
- Current environment limitation: this conversation has GitHub access but no loaded editable ChatGPT Site source or Site-editing control. The linked Vercel account currently contains no WellFit project, so there is also no existing Vercel preview that can substitute for target-Site visual acceptance.
- Parallel current work: `WFG-VIS-001` canonical visual-baseline reconciliation remains open/stale but resumes only after Rudi is visually accepted; do not start a third Landingpage/Rudi implementation.
- Governance blocker: WellFit `main` remains remotely unprotected; branch+PR discipline continues. The owner branch-protection action is deferred and must not be repeatedly requested.
- Falsification question: if the exact public Site preview shows Rudi floating, detached footing, remaining onscreen after his bound surface leaves, unexplained teleportation, air-climbing, broken foreground/background occlusion, obscured controls, pointer interception, delayed relocation after reduced-motion/static fallback, or other motion despite reduced-motion preference, the implementation is not accepted and must be corrected before publication.

Future handoffs must record updated time, active IDs, exact last verified result, first unproven step, blockers/open loops, failed-attempt references, evidence/PR/commit references and any user input still required.
